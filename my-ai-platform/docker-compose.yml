version: "3.8"
services:
  api:
    build: ./packages/api
    ports:
      - "3000:3000"
    env_file: .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  web:
    build: ./packages/web
    ports:
      - "5173:5173"
    depends_on:
      - api
